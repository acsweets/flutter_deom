enum ChargeType {
  normal,
  rebate,
  discount,
}

enum GameState{
  doing, // 进行中
  redWin, // 红胜
  blueWin // 蓝胜
}
enum CheckModel {
  horizontal, // 横向判断
  vertical, // 竖向判断
  leftDiagonal, // 左斜判断
  rightDiagonal // 右斜判断
}